Installation
============

Installing is very streight forward once VMware is already installed, it's as
easy as

.. code-block:: text

    pip install -U vix

.. note:: You must have a VMware product installed before installing this Python VIX wrapper.
